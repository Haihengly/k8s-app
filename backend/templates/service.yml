apiVersion: v1
kind: Service
metadata: 
  name: backend-service
spec:
  selector:
    app: backend 
  type: ClusterIP
  ports:
    - protocol: TCP 
      port: 5000     # the port your frontend will call
      targetPort: 5000 # maps to containerPort in the pods
      # nodePort: 32500
